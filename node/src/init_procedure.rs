use crate::config::node_config::NodeConfigInstance;

#[allow(unused)]
pub fn send_init_handshake(_instance: &NodeConfigInstance) {
    //TODO get back to this when finish discovery system on controller
}
